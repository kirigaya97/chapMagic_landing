---
interface Props {
    class?: string;
    once?: boolean;
}

const { class: className, once = true } = Astro.props;
---

<div
    class={`reveal opacity-0 translate-y-6 ${className || ""}`}
    data-reveal-once={once}
>
    <slot />
</div>
